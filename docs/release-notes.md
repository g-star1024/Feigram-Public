# Feigram Public 发布说明

## 版本 2.6.25

- 桌面图标显示名称「feigram」改为首字母大写的「Feigram」

## 版本 2.6.24

- 修复群组信息面板错位：点击会话标题查看群组信息时，面板现在以右侧固定抽屉展示（此前缩在窗口左下角被裁掉）

## 版本 2.6.23

- 下载列表与后台缓存列表的错误提示净化：只保留中文说明，英文技术链移入悬停提示
- 手动下载的任务现在也会进入「资源库」
- 后台缓存面板优化：标注下载引擎、保守模式下并发固定为 1 的说明与展示修正
- 账号管理：重新登录按钮移到退出按钮之前
- 桌面图标名称改为 feigram
- 修复网络抖动时 waitSession: connection dead / engine forcibly closed 被误判为终态失败

## 版本 2.6.22

- - 升级后不再需要手动重试：此前因 FLOOD_PREMIUM_WAIT（免费账号带宽限流）被误判终态失败的任务，升级到本版会自动复活为排队并续传（2.6.21 升级后下载页躺尸的根因：升级复活清单漏了这个新错误文案）
- - 手动「开始」仍可用：强制重启 + 断点续传

## 版本 2.6.21

- 修复：普通 FLOOD_WAIT（420）被误报成「免费账号带宽配额 FLOOD_PREMIUM_WAIT」，等待原因显示错误
- 修复：普通限流不再污染 premium 计数，避免无辜触发下载并发减半
- 说明：免费账号的 FLOOD_PREMIUM_WAIT 处置（按 Telegram 秒数精确等待、长等待交由任务层、反复限流降并发）保持不变

## 版本 2.6.20

- 【免费账号下载限流不再直接判失败】Telegram 对免费账号的下载带宽限流会返回 FLOOD_PREMIUM_WAIT（括号里的数字就是「请等 N 秒」），而 gotd 与我们的限流识别此前都不认识这个错误码，任务推进到几百 MB 后会直接终态失败；现在按 Telegram 给的秒数精确等待后自动续传，等待原因会显示在任务里。
- 【等待分两层，不误撞看门狗】3 分钟以内的等待在下载器内部就地等待并重试同一个请求（4 路并发各自独立等待，代价最小）；超过 3 分钟的等待交给任务层按秒数精确等待，避免在下载器里空等被无进度看门狗误判成「挂死」而杀掉任务。
- 【反复限流会自动降并发】同一账号累计 premium 限流 ≥3 次 → 并发减半，≥6 次 → 单线程；某一轮下载完整跑完且一次限流都没撞上即恢复原并发。免费账号的带宽配额是固定的，4 路并发只会持续撞限流、把时间耗在反复等待上，降档后总吞吐反而更高。

## 版本 2.6.19

- 【下载不再「假完成」】修掉一条「文件与自己比较」的完成判定：任务未声明大小时（照片等），此前只要落了任意字节就被判成已下载完成并改名交付；现在必须再由一次 4KB 探测确认该位置之后确实没有数据，否则拒绝交付并继续续传。
- 【分片大小不再越界】官方下载器把「本次返回字节数 < 请求分片大小」当作文件末尾，因此分片有硬上限 512KB；此前把配置里的 1MB 直接传了进去，第一块就会被误判成「已到末尾」、官方随即以成功返回——任务看着跑完了，实际只落了一小块。现在分片被夹在 512KB 以内，断流自适应阶梯也改为 512KB → 256KB → 128KB。
- 【官方说「到末尾」也要独立复核】官方下载器报成功只代表它遇到一次短读，而短读也可能是链路把这一块截断了。现在成功之后还会发一次 4KB 小请求独立确认：真末尾返回空块，仍能取到字节就继续续传（最多复核 5 次，避免空转）。
- 【有洞的文件不再被交付】并发分片是乱序写入，交付前会把 .part 裁到「从 0 起的连续前缀」，杜绝「大小对得上、内容有空洞」的坏文件。
- 【会话列表完整性不再误报】此前用「去重后条数 ≥ 扫描上限」推断是否被截断，只要翻页出现重复项就必然失效——实测扫描 500 条、去重后 494 条、服务端共计 545 条，日志却打印「完整拉取完成：494 条（服务端共计 545 条）」。现在按「已扫描条数 vs 服务端上报总数」判定，不足即明确标注【不完整列表】并说明差多少。
- 【照片任务的大小不再缺失】Go 原生元数据里没有 message.file、照片也没有 document.size，导致照片任务的文件大小恒为 0（这正是上面那条假完成判定的入口）；现在照片会取最大一档 PhotoSize 的 size。
- 【日志更可判】下载到达末尾时打印「连续前缀 / 声明大小 / 官方报告的末尾 / 请求次数」；交付前若裁剪过 .part 也会明确记录，便于一眼确认文件是否完整。

## 版本 2.6.18

- - 【下载内核更换】改用 gotd 官方 telegram/downloader：并发分片（4 路并行拉取）+ 分片级重试（FLOOD_WAIT 按 Telegram 秒数等待、TIMEOUT 立即重试），取代此前手写的单线程顺序取片循环。官方实现同时保证分片 4KB 对齐、默认 512KB 分片。
- - 【退避分级：断流不再罚站】「服务端未确认（retryUntilAck retry limit reached）」这类链路断流改用短退避 5 秒 → 60 秒封顶并立刻重建媒体连接（2.6.16 实测同样是断流却要等 2 分 40 秒，1.5GB 文件要跑十几小时）；FLOOD_WAIT 限流仍按 Telegram 给的秒数精确等待。
- - 【同一 DC 反复断流会降级】某媒体 DC 连续断流 2 次后，在候选序列中降级（只降级不排除，兜底路径仍在）；该 DC 重新跑出流量即恢复首选。
- - 【分片自适应】一次尝试内连续断流时，分片自动从 1MB 缩到 512KB、再缩到 256KB —— 大分片在丢包链路上「断一次就整片重来」。
- - 【断点续传对官方下载器透明】请求分片偏移与写入偏移同时平移，已下载部分不会重下；并发乱序写入用「已完成区间合并」求出连续前缀作为续传点，不会在文件里留下空洞。
- - 【数据安全】下载中文件改为读写模式打开（并发分片写与追加模式互斥）、失败时裁到连续前缀、完成判定改用连续前缀而非文件大小 —— 避免「有空洞的文件被当成完整文件」交付。
- - 【看门狗适配】已开始传输后的无进度窗口从 2 分钟放宽到 5 分钟：官方下载器会在内部静默等待 FLOOD_WAIT，原窗口会把「正在按规矩等限流」误判成挂死。首字节窗口仍保持 30 秒快速失败。

## 版本 2.6.17

- 修复会话列表翻页游标：messages.getDialogs 的 offset_date 在 TL 里是无条件字段（官方迭代器每页都会带上它），此前只传 offset_peer + offset_id、offset_date 恒为 0，服务端因此定位不到「上一页末尾」，每页都从列表开头重发——实测 4 页共取回 400 条，按会话去重后只剩 102 条，这才是「群组列表还是不全」的真因。
- 翻页终止判定改用服务端上报的总数（messages.dialogsSlice.Count）：取满即收工，不再用「本页不满一页」去猜末尾，避免漏页与空翻。
- 翻页途中遇限流不再直接截断：FLOOD_WAIT 在 30 秒内会等待后继续翻页（此前第 5 页 FLOOD_WAIT(24) 直接收工，列表被永久截断）；页与页之间加 200ms 间隔，避免连发自造限流。
- 新增逐页诊断日志：每页记录「请求条数 / 返回条数 / 新增条数 / 累计条数 / 服务端共计 / 下一页游标」，翻页是否真的在前进一眼可判。
- 列表不完整时显式标注【不完整列表】并说明已返回多少条、服务端共计多少条，不再静默返回一个短列表。
- peer 索引深翻（backfillNativePeerIndex）同步修正同一处游标缺陷——它此前同样漏传 offset_date，这也是 peer 索引长期涨不上去的原因。

## 版本 2.6.16

- 修复 2.6.15 安装包依赖树不完整导致的「本地应用启动失败」：构建时 npm ci 静默丢了 11 个文件，其中 engine.io/build/parser-v3/index.js 是 socket.io 的加载期依赖，node 一启动就 MODULE_NOT_FOUND 秒退，飞牛便报启动失败。
- 本版重建完整依赖树；下载器与前端功能与 2.6.15 完全一致（DC 迁移修复、AUTH_BYTES_INVALID 转瞬态、会话分页韧性等全部保留）。
- 新增依赖树守卫：逐个 require 生产依赖、校验 node_modules 文件数下限、真实启动服务并断言 /api/health 返回 200。
- 守卫已接入构建流程与 FPK 核证，判据从「版本号对得上、文件存在」升级为「依赖图能加载、服务真能起来」；用 2.6.15 坏包跑守卫可稳定复现失败，正负例均已验证。
- 启动失败原因落盘：启动脚本失败时把原因与日志尾部写入 feigram.log，不再因飞牛吞掉标准输出而无从定位。

## 版本 2.6.15

**R4.37 · DC 迁移修复 + 分页韧性（2026-09-24）**

- **FILE_MIGRATE 两种形态均识别**：gotd 会打出 `FILE_MIGRATE (1)`（空格括号形态），此前正则只认 `FILE_MIGRATE_1` 下划线形态 → 迁移指令被当普通瞬态，任务在错误 DC 与文件所在 DC 之间打转（2.6.14 实测 6f32 复现）。现在命中即切目标 DC 续传，带 3 次迁移预算防循环。
- **AUTH_BYTES_INVALID 转瞬态**：export/import 授权字节被目标 DC 拒收（多为代理连接损坏），此前可能一票终态；现在可读文案 + 自动重试。
- **会话分页韧性**：真分页后中间页失败会重试一次，仍失败则返回已拉取的部分结果（此前任何一页失败整单报废，前端列表全空）。

## 版本 2.6.14

- ### R4.36：群组列表显示不全的根因修复（用户 2.6.13 实测反馈）
- **根因**：`messages.getDialogs` 的**单页硬上限是 100 条**，而代码把
  `maxChatDialogLimit = 500` 当成「单次可拉取量」——服务端只回 100 条，
  于是**第 100 条之后的会话/群组永不出现**（用户实测「群组列表显示不全，
  Telegram 客户端正常」）。同一个误判让 `backfillNativePeerIndex` 用
  `len(dialogs) < 500` 判断「已到列表末尾」，**深翻分页一轮都没翻出去**——
  这才是 `Channel:2052039292` 一直解析不出来的真正原因。
- **修复**：
  - `fetchNativeDialogs` 改为按 `offset_peer + offset_id` **真分页累加**
    （每页 100，直到取满总量上限或列表末尾），并保留截断时的可操作日志提示。
  - `backfillNativePeerIndex` 的满页判定改用真实页大小；深翻轮数上限 6 → 30
    （覆盖 3000/文件夹）。
  - 新增 `TestDialogPaginationGuards` 守卫：防止 `dialogPageSize` 被改大到
    硬上限之上而重新引入「截断被误判为末尾」。
- ### 同期修复（R4.36 A~E）
- **peer 解析冷却分级**：限流/传输类失败 → 账号级退避；结构性「找不到会话」
  → 只退避该 peer（不再冻结整个账号的解析，2.6.13 实测缺口）。
- **连续 3 次结构性失败 → 可操作终态**（`peerUnreachableError`）：明确告知
  「频道已不在会话列表，请重新加入后点重试」，手动重试重置计数。
- **索引命中校验 accessHash**：贫信息（缺 accessHash 的 channel）不再短路
  返回，避免解析逻辑在空 accessHash 上原地绕圈。
- **调度前感知解析冷却**（C）：冷却期内不启动任务，省掉建连 + 选 DC + 探测。
- **下载进度心跳**（D）：每 30s 一条进度日志，终结「静默两分钟后只有
  context canceled」的判读盲区。
- **选 DC 前的真实 RPC 探针**（E）：`mediaDCCandidates` 按探测结论优选候选
  （握手成功且耗时短的优先），并逐个用 `help.getConfig` 做真实加密往返验证；
  探针只做优选，全部失败仍兜底使用可连上的 DC，不阻断下载。

## 版本 2.6.13

- ### R4.35 限流自噬修复（用户 2.6.12 实测反馈）
- - **RPC 传输层失败转瞬态**：2.6.12 实测（10:42:00）网络抖动时
  `invoke pool: rpcDoRequest: retryUntilAck: retry limit reached after 5 attempts`
  被当终态（首个失败即 error）。现把 `retry limit reached` / `retryuntilack` /
  `engine was closed` 加入瞬态表；load() 归一化把 2.6.12 落盘的此类终态任务
  升级时自动复活。
- - **peer 解析账号级闸门**（`peerResolveGate`）：2.6.12 实测三个任务同时解析同一个
  缺失 accessHash 的频道（Channel:2052039292），各自全量深翻会话列表 → 同时触发
  `FLOOD_WAIT (9)` / `FLOOD_WAIT (6)` → 5~10s 后再次深翻 → **限流被自己喂大**。
  现在：并发解析只执行一轮真实深翻（singleflight，结果共享）；失败后账号级冷却
  （基础 60s，`FLOOD_WAIT` 按 Telegram 秒数，封顶 15 分钟），冷却期内直接返回
  可读原因、不发 RPC；深翻失败原因保留进最终错误，使 `floodWaitFromError`
  能提取精确等待秒数。
- - 本轮 2.6.12 已验证生效的部分：健康检查自动恢复（572ms）→ 任务自动启动 →
  常驻连接 510ms 建立 → 探测全绿自动复活（`network healed: 1 个…`）→ 调度紧循环
  冷却 2m（R4.26）→ FLOOD_WAIT 精确等待。链路自愈闭环成立。
- - 节点转发质量仍不稳（探测在「全部握手正常」与「5/5 全黑洞」间摆动），
  建议更换节点。

## 版本 2.6.12

- ### R4.34 网络自愈补漏（用户 2.6.11 实测反馈）
- - **「媒体连接建立超时」转瞬态**：2.6.11 实测（09/24 09:10）网络抖动时任务第一次
  45s 连接建立超时即一票终态（瞬态表只有英文 `timeout`，盖不住中文文案），一次自动
  重试机会都没拿到。现把「媒体连接建立超时/启动失败/建立失败」加入
  `transientSourceError` 瞬态表，按退避自动续传；load() 归一化把 2.6.11 落盘的
  此类终态任务升级时自动复活。
- - **AutoRevived 按「断网窗口」重臂**：此前自动复活每任务一次性，复活后撞上网络
  恶化即永久失去资格（实测：启动复活→节点恶化→45s 超时终态→此后探测全绿也不再
  拉起）。新增 `rearmNetworkRevive`——探测转差时重置该账号 error 任务的复活标记，
  每个断网窗口结束后仍能自动拉起一次；手动重试/开始也会清标记。
- - **连接建立窗口自适应**：固定 45s → max(45s, 8×最近握手ms) 封顶 120s
  （`mediaConnStartTimeoutFor`，与 R4.32 首字节看门狗同源思路）；超时错误文案附
  该账号最新一轮媒体探测结论，避免「为什么失败」再查一轮日志。
- - **Node 侧可读文案**：启动窗口 Go 忙时「文件夹列表加载超时」由裸
  `DOMException [AbortError]` 转为可读提示。
- - 治本建议不变：本轮失败的直接原因是代理节点转发质量（丢包/限速，昨夜探测每轮
  随机挂不同 DC），建议更换节点或线路。

## 版本 2.6.11

- ### R4.33 网络自愈闭环（用户 2.6.10 实测反馈）
- - **网络自愈自动复活**：媒体 DC 探测确认全部真实可用（TCP+握手均过）时，自动把该账号
  「重试上限」终态任务拉起一轮（`reviveNetworkStalledTasks`，重置 RetryCount，写入
  AutoRevived 防抖字段）。每个任务只自动复活一次——复活后若网络再次恶化打满上限，
  不再自动拉起，避免「探测绿→复活→打满→再复活」的无限循环；此时仍可手动重试。
- - **「找不到会话」陈旧终态自动复活**：R4.30 之前 peer 解析失败会一票终态；索引落盘 +
  会话深翻分页后该错误已转瞬态，load() 归一化把落盘的陈旧终态升级时自动复活
  （与 metadata refresh url / DC_ID_INVALID 复活同模式）。

## 版本 2.6.10

- ### R4.32 下载换轨：MediaOnly → 主 DC static 地址（用户 2.6.9 实测反馈）
- - **媒体连接换轨（核心）**：2.6.9 实测确认探测（主 DC IP）握手正常但下载仍 0 字节，
  根因是两者拨的不是同一类 IP——gotd `MediaOnly` 只连 config 里标记「媒体专用」的
  IP（常与主 DC static IP 不同网段），代理未转发该网段时拨号挂死到被看门狗掐断
  （0 字节 × 120 次重试终态）。现改走 `client.DC`（resolver.Primary → config 的
  static 主 DC 地址），与 MTProto 分级探测同一地址类；upload.getFile 在授权连接上
  与 MediaOnly 完全等价，授权导出/导入由连接池自动完成。
- - **探测去抖**：握手超时 6s→10s + 失败重试一次（`probeTelegramMTProtoSteady`），
  消除「每轮随机挂掉一个不同 DC」的抖动误报；部分转发结论措辞区分「节点波动」
  （偶发、重试后仍失败的单 DC）与「网段黑洞」（同 DC 持续失败）。
- - **首字节看门狗自适应**：按该 DC 最近探测的 MTProto 握手耗时 ×4 放宽首字节窗口
  （`latestMediaHandshakeMs`，封顶 90s）；无探测数据时保持 30s 快速失败档。
- - **终态指引**：重试上限后的失败文案改为「请在代理放行 Telegram 全部网段或更换
  节点后点『重试』，将从断点续传」。

## 版本 2.6.9

- ### R4.31 诚实探测升级：MTProto 真握手 + DC_ID_INVALID 复活（用户 2.6.8 实测反馈）
- - **MTProto 真握手探测（核心）**：TCP 探测经代理时是假阳性——本地代理（127.0.0.1）
  接受连接只需 1ms，不代表真的把流量转发到了 Telegram。2.6.8 实测五个 DC「1ms 全绿」
  但下载 0 字节：代理只放行了主 DC 网段，媒体 DC 网段被本地接受后黑洞。现新增真实
  MTProto 密钥交换探测（匿名握手、不携带账号会话、不做授权 RPC、不触发限流），
  「代理只本地接受、未真正转发」被一锤定音识别，任务错误与诊断页直接给出修法：
  放行 Telegram 全部网段、改全局模式或更换节点。健康检查的媒体 DC 全量探测与下载
  0 字节前的单 DC 诊断均升级为 TCP+MTProto 两层。
- - **DC_ID_INVALID 陈旧终态自动复活**：2.6.8 之前「export 授权导到账号主 DC」bug
  留下的失败任务升级后自动复活续传；DC_ID_INVALID 同时进瞬态表，未来再现时自动
  续传而非终态（真实持续失败由重试上限兜底）。
- - **日志瑕疵修正**：媒体 DC 连接失败日志此前恒打印 `media DC 0`（目标 DC 被失败
  路径重置后再打印），现打印真实目标 DC。

## 版本 2.6.8

- ### R4.30 主 DC 解析修正 + 会话指纹 + peer 索引落盘（用户 2.6.7 实测反馈）
- - **主 DC 解析修正（核心）**：Go 侧此前按错误的顶层格式解析落库 session
  （gotd 实际落库为 `{"Version":1,"Data":{"DC":N}}` 嵌套形状），账号主 DC 恒为
  「未知」——健康探测被跳过、媒体 DC 等于主 DC 时的短路失效，导致 DC_ID_INVALID
  （导出授权给自己被拒）与无谓的媒体 DC export FLOOD_WAIT。现按 gotd 真实嵌套
  格式解析（兼容历史平铺格式），健康分级探测、主 DC 短路、诊断探测三处能力恢复。
- - **会话指纹防误重建**：常驻媒体连接与主连接/健康检查共用 session 存储，
  连接握手后 gotd 会回写新盐值的 session，逐字节比对会把「自己回写」误判成
  「重新登录」而重建（实测存活 3 秒即重建）。改按 AuthKeyID 认证指纹判定，
  只有真正重新登录才重建。
- - **peer 索引落盘 + 会话深翻分页**：频道 accessHash 索引此前是纯内存结构，
  重启即空，file_reference 刷新常以「找不到会话 Channel:xxx」终态失败。现索引
  随更新原子写盘、启动恢复；首批 500 会话未命中时按 offset 分页深翻（每文件夹
  最多 6 轮 ≈ 3000 会话）；「找不到会话」转瞬态错误自动续传。
- - **调度可见化修正**：并发坑满时调度循环不再 break 跳过后续任务，让路/冷却/
  缺路径等等待原因对全部排队任务可见（修复偶发的 auto 任务无让路原因）。

## 版本 2.6.7

- ### R4.29 下载/缓存一劳永逸三件套（用户拍板方案）
- - **账号级常驻媒体连接**：每个账号只建一条 MTProto 常驻连接，每个媒体 DC 只
- exportAuth（授权导出）一次，所有下载/缓存任务复用——根治反复授权触发
- FLOOD_WAIT（Telegram 24 分钟限流）的结构性问题；重新登录或删除账号时自动重建/回收。
- - **媒体 DC 分级探测**：健康检查时自动对 DC1–5 做全量 TCP 探测，诊断页直接显示
- 「代理放行了哪些 DC、没放行哪些」；下载 0 字节看门狗终止前也会对该任务媒体 DC
- 做单点探测，结论直接写进任务错误文案。
- - **后台缓存让路**：手动下载永远优先；后台缓存只在无手动任务等待时启动、
- 同时最多一个在跑、两次调度至少间隔 2 秒，避免缓存与手动任务抢同一账号的
- 授权配额。

## 版本 2.6.6

- ### R4.28 折叠目标修正 + 0 字节快速失败（用户 2.6.5 实测反馈）
- - 折叠目标修正：只收起群组分组栏（文件夹图标轨），账号行与会话列表保持可见；
- 收起后在原位留窄展开条，状态记忆保留。
- - 0 字节快速失败：本次尝试一个字节都没收到时 30 秒即转瞬态重试（原 120 秒），
- 媒体路径不通时不再让单个任务白占单并发队列 2 分钟；收到过字节的任务仍用 120 秒窗口。
- - 看门狗日志区分两档原因：「连接媒体服务器后始终无响应」vs「媒体路径无进度」。

## 版本 2.6.5

- ### R4.27 修复 file_reference 刷新链路（用户 2.6.4 实测反馈）
- - 刷新链路自愈：频道类任务缺 accessHash（旧任务/贫信息缓存）时，自动经本地 peer
- 索引 + 回拉会话列表补全后再刷新消息元数据，不再以「metadata refresh url is empty」终态失败。
- - 删除 Node 元数据桥自环回退（其内部仍调 Go 原生接口，Go→Node→Go），直接透传真实错误。
- - FLOOD_WAIT（420）按 Telegram 给出的秒数精确等待（封顶 4 小时），不再套用 5 秒起步的
- 指数退避反复撞墙加深限流。
- - 升级后自动复活 2.6.4 中被误判终态的「metadata refresh url is empty」任务。
- - 会话页侧栏支持收起/展开（localStorage 记忆）。

## 版本 2.6.4

- ### R4.26 修复日志风暴：找到 2.6.2/2.6.3 症状的总根因（用户 2.6.3 实测反馈）
- - 总根因：**任务状态写在了拷贝上**。调度器启动任务时把「下载中」状态写到列表快照（值拷贝）里，
- 存储任务永远停在「排队中」——下一轮调度立即把刚启动的任务当僵尸清理再重新启动，
- 形成启动/误杀死循环。这同时解释了 2.6.2 的「永远排队、运行 0/1」和 2.6.3 的
- 「同一秒刷出几十条 stalled/failed 日志」（goroutine 在取消生效前竞态存活，多个并发写同一任务，
- 同窗口启动的看门狗在同一秒齐爆）。
- - 修复一（根因）：状态落到存储任务，任务启动即真实进入「下载中」，不再被误杀重走。
- - 修复二（调度限流）：同一任务 10 秒内最多重启 3 次，超过进入指数冷却（最长 5 分钟），
- 无论上游是谁在反复复活任务（后台缓存轮询/手动点击），紧循环都被掐断。
- - 修复三（瞬态化）：「媒体源返回空响应（0 字节）」与 context deadline exceeded 不再记终态，
- 改为按退避自动续传。
- - 修复四（日志限频）：同一任务同一条错误 60 秒内只打一条，其余计数后汇总。
- - 修复五（复活收敛）：已取消任务不再被后台缓存轮询擅自复活；失败任务复活保留退避计数。

## 版本 2.6.3

- ### R4.25 修复「任务一直排队、点开始没反应」（用户 2.6.2 实测反馈）
- - 根因：**调度僵尸坑位**。下载协程在媒体路径挂死时不退出，却持续占用并发名额；「开始」只把任务改回排队、
- 不清旧协程，于是单并发下整条队列被静默冻死——界面显示「运行 0/1 + 全部排队中」，日志零输出。
- - 修复一（自愈）：每次调度前识别并清理僵尸坑位（任务状态已非下载中/已删除）并取消其旧协程，无需重启服务。
- - 修复二（强制重启）：「开始」先取消该任务仍存活的下载协程，再重新排队。
- - 修复三（看门狗）：原生下载连续 120 秒零字节即判定媒体路径挂死，转为瞬态错误并按退避自动续传；
- 错误文案直指根因（代理是否放行 Telegram 媒体 DC 网段）。
- - 修复四（可诊断）：任务启动/完成留日志；`FilePath` 缺失不再静默跳过而是写入可见等待原因；
- 运行诊断页新增「调度」条目（在跑数 / 坑位数），坑位异常显式告警。
- - 修复五（版本对齐）：Go 下载器版本改由 `-ldflags -X main.version` 注入，日志首行与安装包版本一致。

## 版本 2.6.2

- ### R4.24 会话侧栏：文件夹竖排图标栏（用户拍板方案 3）
- - 文件夹切换从横排胶囊行改回**左侧竖排图标栏**（类 Telegram 布局）。
- - 只放图标 + 未读角标（标题走 tooltip/悬停提示），避开旧 82px 竖轨的文字竖排挤压问题。
- - 竖轨仅占 64px 宽、按钮 44×44（24px 体系圆角 token），文件夹再多也不挤压会话列表、不横向滚动。
- - 附带：`.sidebar-main` 由固定 82px 首列的 grid 改为 flex（文件夹关闭时不再空出首列）。

## 版本 2.6.1

- ### R4.23 下载缓存修复 + 界面简化
- - **修复下载/后台缓存「失败后不再续传」**
- - 根因：账号未就绪时全局传输配置被降级为 http-bridge，而它的下载源指向 Go 自身的 blob 端点（自环），
- 「尚未就绪」被包装成 `source returned 404` 终态错误——账号恢复后任务也永远拉不起来。
- - 修复：Go 不再降级传输配置；存量 blob 自环任务自动升级回 Go 原生 MTProto；blob 404 中的「尚未就绪」
- 归类为瞬态错误，账号恢复后自动续传；新增任务一律原生 MTProto。
- - **界面简化**
- - 账号卡片：「扫码登录 Go / 健康检查 / 验证码兜底」三个按钮收敛为一个「重新登录」
-（扫码面板内提供「改用验证码登录」兜底）；健康检查已自动化，手动入口保留在运行诊断页。
- - 设置页：移除「媒体源传输层」下拉与「媒体源」统计（单一 Go 原生 MTProto 后无意义），更新过时提示文案。
- - 会话侧栏：移除文件夹「编辑」按钮（文件夹管理在 Telegram 客户端内完成，同步开关在隐私设置）。

## 版本 2.6.0

- ### R4.22 架构简化：健康检查只做一件事，下载错误直出真实原因
- - **健康检查收敛为「授权成功 = 健康」**
- - 删除整层「媒体抽样」（自动找缩略图、读 64KB、验证媒体池）。该层自 R4.17 起已不参与
- 健康判定，却是 DC_ID_INVALID 等假故障的主要来源，属纯冗余。
- - 一次授权 RPC 通过即就绪，消除「界面显示健康、下载却报账号未就绪」的中间态。
- - 健康检查的开始/结束与分级探测结论都写入服务端日志（`health check start/end`、
- `health probe`），不再有零输出路径。
- - **下载错误直接透出真实原因**
- - 移除「账号不可用 → 自动回退 HTTP 桥」链路。M4.1 之后该「回退源」指向 Go 自身的
- blob 端点，回退只会绕回同一个坏账号，把真实原因包装成
- `source returned 404: {"error":"Go 原生账号 … 尚未就绪…"}` 这类三层转述。
- - 账号不可用时任务留在队列并显示可读原因（直接带上健康检查诊断结论），
- 账号恢复后自动续传，无需手动重试。
- - **分级探测补盲区**：拿不到账号主 DC 时不再静默跳过，错误信息里必带「分级探测：」结论，
- 便于区分「探测被跳过」与「诊断没生效」。
- - **保留**：R4.18 媒体主 DC 直连（根治 DC_ID_INVALID）、R4.20 出口链路/MTProto 分级探测。

## 版本 2.5.6

- - 启动即对「有会话但未就绪」的账号自动补跑健康检查（原先要等 30 分钟巡检或 3 分钟访问冷却）
- - 升级/重启后新版诊断（分级探测结论）秒级可见，不再先看到旧版落盘的错误信息

## 版本 2.5.5

- - 健康检查新增分级探测：先用与 MTProto 相同的代理出口对账号主 DC 做裸 TCP 拨号（10s），把「代理/出口链路不通」与「MTProto 握手/授权卡死」拆开
- - 探测失败 → 明确提示「问题在代理出口链路（未放行该 DC 直通/节点故障/端口配错）」；探测成功但超时 → 明确提示「TCP 层已通，多为节点转发质量差，建议更换节点」
- - 探测结果同时写入服务端日志（health probe 行）与账号错误详情，替代笼统的 context deadline exceeded
- - 新增 primaryDCAddr（gotd 生产 DC 表，静态 IPv4 优先）与 6 组单测；Go 全量测试通过

## 版本 2.5.4

- 修复会话页「visible is not defined」加载失败错误卡（历史重构遗留的未定义变量引用，开启「文件夹自动选第一个会话」时必现），该设置恢复生效
- 下载目录权限不足时给出可操作提示（给目录开放写权限或改用应用可写目录），不再裸报 permission denied

## 版本 2.5.3

- 根治媒体抽样/下载的 DC_ID_INVALID：目标媒体 DC 与账号主 DC 相同时改用主连接直读，不再经 gotd 媒体池「导出授权给自己」
- 健康检查自动抽样、缓存任务抽样、下载 worker 三条路径统一接入主 DC 短路，避免无意义的跨 DC 建池与授权往返
- 新增主 DC 解析（从加密落库 session 读取），解析失败自动回落原有行为，不影响老账号

## 版本 2.5.2

- 修复：健康检查分级——session 授权成功即账号可用，媒体抽样失败（如 DC_ID_INVALID）不再把账号打成 failed 堵死会话列表
- 优化：DC_ID_INVALID 映射为可读提示（媒体 DC 授权导出被拒，账号本身可用）
- 修复：基础检查通过也推进 Ready 恢复计数，此前「无媒体可抽/抽样失败」的账号永远无法恢复就绪

## 版本 2.5.1

- 修复：「尚未就绪（failed）」错误现在会附带健康检查的真实失败原因（如代理连不通/DC 超时），不再只有状态词

## 版本 2.5.0

- 下载中心整合：原管理后台「缓存信息」整块迁入下载中心，成为「下载任务 / 后台缓存」两个子标签
- 后台缓存的设置项（开关·最大速率·缓存模式·并发）、运行统计（运行中/传输层/任务数）、任务列表、批量取消与拖拽排序全部保留
- 首页统计卡可点击直达：进行中/已完成下载 → 下载任务分区，缓存任务 → 后台缓存分区

## 版本 2.4.6

- 修复：应用重启后 failed 账号不再干等 30 分钟——每次配置同步（含启动回推）都会立即全量重检
- 优化：代理设置提示明确 v2rayA 端口配对（HTTP=20171、SOCKS5=20170），避免协议错位连不上

## 版本 2.4.5

- 修复：账号因网络不通被判 failed 后，代理一改立即自动重检，不再等 30 分钟
- 修复：访问未就绪账号时自动安排健康检查（3 分钟冷却），聊天页可更快自愈
- 修复：头像拉取失败改为显示占位图，日志不再刷 "Go downloader 404"
- 修复：媒体源长时间不可用的任务最多自动重试 120 次后停止，不再无限刷日志

## 版本 2.4.4

- 重复账号根治：同一手机号重新登录后自动清理旧记录，不再出现同号 healthy/failed 双卡片；当前账号未就绪时会自动切换到可用账号，会话页不再整页报错。
- 下载健康度可观测：账号卡新增「连续失败 N 次 / 最近成功时间」，运行诊断与 /health 增加「隐性退化」预警（degraded）。
- 排队透明化：因账号未就绪而等待的下载任务，现在会显示原因（「将在账号恢复后自动继续」），不再静默卡住。
- 日志增强：运行诊断的日志支持一键导出 .txt、新增「跟随最新」自动滚动开关。
- 代理说明：设置页补充「本机已全局科学上网时直接填本机代理端口」的用法说明。

## 版本 2.4.3

- 首页新增「最新版本」卡片：Dashboard 直接展示最新版本与更新亮点；快捷操作移除与顶栏重复的公告入口，完整公告仍从顶栏铃铛查看。
- 修复：管理后台「隐私设置」页打开白屏的问题（上一版引入的显示缺陷）。

## 版本 2.4.2

- 公告瘦身：应用内公告历史只保留最近 5 条版本，避免列表越积越长；完整更新说明仍可在 docs/release-notes.md 查看。
- 通知权限入口优化：桌面通知的权限申请从公告铃铛移到「设置 → 隐私 → 通知设置」，点铃铛只管看公告，不再弹浏览器授权窗；被拒绝时页面给出恢复指引。
- 工程清理：删除已废弃的旧版打包脚本，发版只走 build-native-fpk.sh 一条路径。

## 版本 2.4.1

- 诊断日志分级着色/级别与关键词过滤/清空按钮 + 管理后台服务端/隐私设置 tab 视觉对齐诊断页 + 双端窄屏布局核查

## 版本 2.4.0

- Feigram 2.4.0 常规更新

## 版本 2.3.1

- 会话列表加载更稳：慢网络下放宽等待，让下载服务的具体报错（而不是一句无指向的超时）能正常返回。
- 默认只拉当前会话、不再每次重复拉取归档文件夹，列表加载更省更快；开启「显示归档会话」时才一并拉取。
- 加载超时时给出可读说明，并引导到「设置 → 网络代理」核对网络，而非显示无法理解的英文错误。

## 版本 2.3.0

- 健康检查自动化：登录成功后自动执行健康检查；无缓存文件时自动抽样低体积媒体验证文件池；每 30 分钟定时巡检，账号状态变化实时推送到界面，无需手动刷新。
- 媒体源自动升级：有账号通过健康检查后，自动把媒体源从 HTTP 桥接切换到 Go 原生 MTProto，兑现原生下载速度；每进程只自动升级一次，仍保留手动回退。
- E2E 回归入库：端到端脚本整理到 scripts/e2e（含零预置数据的首次登录路径），支持 npm run e2e 一键全量回归，便于后续持续核证。
- 发版工具化：新增 scripts/bump-version.sh 一键同步版本五处真源、scripts/verify-fpk.sh 解包核证，避免版本号漏改。

## 版本 2.2.0

### 重构（管理后台）

- **运行诊断**：卡片/徽标对齐设计 token（`--fn-radius` 12px、语义色 pill）；Node/Go 日志改为折叠展示（默认收起，不再被历史运行日志刷屏）；删除裸 JSON dump 与「LIMIT_INVALID/请求分片」类行话指标；诊断概要去掉与下方重复的「数据目录」「版本线」。
- **缓存信息**：运行概要由 4 格减为 3 格（去掉与下方选择器重复的「并发设置」）；无任务时隐藏「全选当前/清空选择/取消选中」批量按钮。

### 修复

- **登录前合规提示静默缺失（M1.3 回归）**：前端消费 `settings.telegramLegalNotice`，但后端 `publicSettings` 从未输出该字段——登录弹窗的 Telegram 服务条款提示在所有已发布版本中均为空。现已补上（常量 `TELEGRAM_LEGAL_NOTICE` 一直存在，仅供给链断裂）。
- **健康检查误报失败**：已授权 session 因「没有缓存视频可抽样」被硬标 `failed`。现改为：`Auth().Status` 通过后若无缓存文件任务，记为基础检查通过（保持 Ready/HealthPasses 原样，文件池抽样待有缓存后自动补做）。
- **重复/残留账号记录**：退出登录现在会同步删除该账号的 Go 会话记录；诊断页把「无对应应用内账号」的孤儿记录单独分组并提供一键清理（`DELETE /api/native/accounts/:u/:a` 新增）。
- **聊天页默认账号**：默认选择「已就绪」账号而非列表第一条，不再因旧失败账号整页报「尚未就绪」。

### 验证

- Go 全量测试 ok；Node 37/37；前端构建通过；真实二进制 E2E 核证 DELETE 链路（建档→删除→GET 404→持久化文件无残留）。

## 版本 2.1.3

### 修复

- **首次登录 100% 失败（`native account is not prepared`）**：`POST /api/auth/start`（登录弹窗「发送验证码」）由 Node 生成全新 `accountId` 后直发 Go，而 Go 侧 `startNativeLogin` 要求该账号记录已存在——全新账号永远没有记录，导致**所有**首次登录必失败；扫码登录（`/api/auth/qr/start`）同样命中。2.1.2 及之前版本全部受影响。
  - **修复**：登录入口新增 `ensureNativeAccountLocked`，缺记录时用请求自带的手机号 + API 凭据自动建档；凭据缺失时报明确的 `apiId/apiHash is required`，不再报含糊的 not prepared。
  - **为何此前未被发现**：E2E 脚本预先 `POST /api/native/accounts` 建过档，判据盲区掩盖了首次登录路径。
- **验证**：新增 3 条建档单元测试；真实二进制 E2E（全新数据目录、零预建档案）6/6 全绿——缺凭据明确报错、全新账号走到网络层并返回 40s 超时 + 代理诊断提示、记录自动落盘含手机号与 apiId。

## 版本 2.1.2

### 新增

- **网络代理支持（原生模式）**：此前 Go 侧 MTProto 链路**完全没有代理能力**（`telegram.NewClient` 默认用 `dcs.DefaultResolver()` 裸直连），因此在不能直连 Telegram 的网络里，原生模式必然表现为「登录一直失败 / 转圈」。现在提供三层配置：
  1. **应用内设置（推荐）**：「设置 → 网络代理」填写 `socks5://` / `socks5h://` / `http://` / `https://` 代理，改完立即生效，无需重启。
  2. **环境变量兜底**：`FEIGRAM_PROXY_URL` > `ALL_PROXY` / `all_proxy` > `SOCKS5` / `socks5` > `HTTPS_PROXY` / `https_proxy` > `HTTP_PROXY` / `http_proxy`。
  3. **直连**：两者都没有时按原行为直连。
- **优先级与生效范围**：应用内设置 > 环境变量 > 直连。MTProto 建连（登录、健康检查、会话、下载）与媒体 HTTP 传输都会走代理。
- **回环豁免**：媒体传输对指向本机的回环地址强制直连，避免用户配了远端代理后打断本机网关 / sidecar 调用。

### 修复

- **代理配置错误不再静默直连**：空主机名、非法协议（如 `socks4`）、非法端口、带路径的地址都会被显式标记为 `invalid` 并在设置页与 `/health` 中报出原因，不再「悄悄退回直连」让用户以为代理生效了。
- **代理不可达时给出可操作提示**：未配代理时提示去「设置 → 网络代理」配置；已配代理仍连不上时提示检查代理进程、核对地址端口、确认代理允许 TCP 直通 Telegram。提示会出现在登录失败与诊断信息中。
- **代理凭据不进日志**：日志与接口只输出 `scheme://host:port`，代理用户名密码一律脱敏。
- **加固登录流程（本次核证中发现的连带缺陷）**：
  - **同一账号重复点登录**：旧流程现在会被取消。此前每次点击都会新起一个 goroutine，两边并行拨号，且旧流程收尾时会把账号状态覆盖成失败。
  - **发起登录超时**：等待验证码上限（40s）到达后立即取消该流程。此前前端早已超时（拿不到 `loginId`，流程在客户端失去意义），服务器端 goroutine 却会继续对 Telegram DC 反复重试。
  - **代理配置变更**：在途登录会被取消。在途登录把旧 dialer 固定在 gotd client 里，不取消就会继续对旧代理拨号重试。
  - **登录流程生命周期**：新增 10 分钟上限，`a.logins` 在流程结束时即摘除，避免登记表随无人轮询的流程堆积。

### 验证

- Go 单元测试：进程内 SOCKS5 / HTTP CONNECT 代理桩，覆盖隧道连通、凭据透传、错误凭据失败、代理拒绝、协议/主机名校验、脱敏、优先级链、回环豁免、gotd 接线（断言 `Resolver` 确实被注入的 dialer 调用）。
- 端到端核证（真实二进制 + 常驻代理桩）：断言代理桩**确实收到指向 Telegram DC 的 CONNECT**，而非仅看接口返回 200。覆盖：环境变量生效、应用内设置覆盖环境变量且旧代理停用、非法地址显式报错、不可达时的提示文案、代理变更后在途登录被取消。

## 版本 2.1.1

### 修复

- **修复「公告弹窗关不掉 / 点铃铛没反应」**，根因有三，均已修复：
  1. **通知权限申请阻塞弹窗**：点击铃铛会先 `await Notification.requestPermission()` 再打开公告弹窗。在部分环境（fnOS WebView、iframe、页面未聚焦等）该请求**长期不返回**（真实浏览器实测永久挂起），导致点击铃铛毫无反应。现已改为先开弹窗、权限申请异步进行并带 8 秒超时兜底。
  2. **关闭按钮几乎不可见**：原关闭按钮仅 30×25 且与深色背景对比度极低。现改为 32px 圆形按钮（描边 + 底色 + hover 变红），并提供**背板点击关闭**；Esc 关闭保持可用。
  3. **弹窗叠加**：公告弹窗与管理后台可同时叠放，上层 backdrop 会吞掉下层弹窗的点击，且 Esc 关闭顺序与视觉层级相反，造成「怎么都关不掉」。现已改为**弹窗互斥打开**（打开公告自动关闭管理后台，反之亦然）。

### 其他

- 应用内「更新公告」列表仍然保留完整历史（本次未清理，是否裁剪历史条目待定）。

## 版本 2.1.0

> 本次为界面焕新的正式发布版本（此前的 2.0.47 构建内容并入本版），并修复了安装包版本号上报问题。

### 修复

- **修复安装包版本号滞后导致升级后不重启**：`manifest` 与 `cmd/main` 的 `APP_VERSION` 此前停留在 `2.0.45`，与包名（2.0.47）不一致。飞牛 OS 会按 `version=` 判定包版本，而启动脚本在「已运行版本 == APP_VERSION」时会直接 `exit 0` 跳过重启，表现为**安装新包后应用没有升级**。现已将包名、`manifest`、`APP_VERSION` 三处版本号统一为 `2.1.0`，升级后会正常重启并更新 `feigram.version`。
- 应用内「更新公告」列表补齐本版内容（此前只更新到 2.0.45）。

### 升级注意

- 从 **2.0.45 及更早**（GramJS 模式）升级上来的账号，会提示「需重新登录以升级到原生模式」，删除该账号重新登录一次即可，**不会影响 Telegram 账号数据**。

### 界面全面焕新（fnOS 设计风格）

- **全新 fnOS 风格界面**：左侧 Dock 导航（首页 / 会话 / 下载 / 资源库 / 管理 / 设置）+ 毛玻璃顶栏 + 应用窗口内容区，视觉与飞牛 fnOS 系统应用一致。
- **亮色 / 暗色双主题等权**：默认亮色，顶栏手动切换并记住选择；支持 fnOS 注入的 `?theme=` 参数与 `fnos_theme` cookie 自动跟随。
- **新增首页 Dashboard**：账号 / 下载 / 缓存统计卡、近期活动、快捷操作。
- **新增下载中心页面**：原侧边抽屉升级为独立页面，任务卡含进度、速度与操作；Dock 图标显示进行中任务数。
- **新增资源库页面**：后台缓存的媒体卡片网格，支持按视频 / 图片 / 文件筛选，点击直接播放或打开。
- **会话与聊天重设计**：文件夹改为顶部胶囊行、气泡与媒体卡片圆角化、输入区毛玻璃质感。
- **管理后台 / 设置 / 诊断 / 扫码登录 / 播放器**统一套用新组件规范（胶囊 Tab、卡片行、语义色状态）。
- **响应式与无障碍**：1024px 以下 Dock 折叠为图标栏、768px 以下转为底部导航；可点区域 ≥44px；模态支持 Esc 关闭并补充 `role="dialog"` / `aria-label`；统一焦点环；动效尊重系统「减弱动态效果」设置。

### 内部

- 前端样式迁移到 `--fn-*` 设计 token 单一真源（tokens.css），移除旧青绿色主题变量与死代码 CSS。

## 版本 2.0.46

### 重要变更（需要部分用户操作）

- **彻底移除 Node 侧 GramJS（`telegram` npm 包）客户端**，Feigram 现在只通过 Go 原生 MTProto（gotd）单客户端连接 Telegram，架构更简单、资源占用更低，也消除了同一账号双端同时握手导致 `AUTH_KEY_DUPLICATED` 的问题。
- **旧版（gramjs 模式）账号将不再可用**：升级后这些账号会显示「需重新登录以升级到原生模式」。请删除该账号后重新登录一次即可转为原生模式；此过程**不会销毁你的 Telegram 账号数据**，云端聊天与媒体不受影响，仅本应用的会话需要重建。
- 已迁移到原生模式（native）的账号不受任何影响，聊天、搜索、媒体播放/下载、后台缓存等能力全部经 Go 原生链路提供。

### 调整

- Node 服务端删除约 2000 行遗留 GramJS 代码路径；登录流程此前已全部改由 Go Telegram Core 处理，本次变更对原生账号的 API 行为无影响。
- 会话保存方式变更：登录后不再保存 Node 侧加密 session，session 由 Go 侧独占管理。

## 版本 2.0.30

### 修复

- 修复后台缓存任务在 Telegram 连接持续抖动时仍长期显示 running 的问题。
- 后台缓存增加持续低速 watchdog：不限速时低于 128KB/s 持续 3 分钟会自动释放运行槽、重置连接并等待断点续传。
- 任务重新排队和重新开始时会清理旧的低速状态，避免前一次卡顿影响下一次续传。
- 缓存速度诊断不再把聚合速度误认为真实测速，会显示低速观察状态和是否疑似卡死。

### 调整

- 诊断窗口在已有后台任务运行时不再额外抢占 Telegram 连接，减少前台聊天和后台缓存互相拖慢。

## 版本 2.0.29

### 修复

- 修复下载列表显示已完成但本地文件不完整，导致视频无法加载或提示格式不支持的问题。
- 用户主动缓存下载改为复用 Telegram 分片续传链路，不再每次开始都删除 `.part` 断点文件。
- 目标文件、后台缓存文件和重启恢复记录都会严格校验实际文件大小，未达到 Telegram 原始大小时自动转回队列续传。
- 高速模式下前台会话、消息、搜索等操作也会短暂暂停同账号后台缓存，避免后台任务把 Telegram 连接拖到 TIMEOUT 后前端列表空白。

### 调整

- 高速模式多任务全 0 速停滞时会降回保守模式，并给重排任务加入短冷却，避免立即重启后再次卡住。

## 版本 2.0.28

### 修复

- 修复缓存速度诊断里“并发/运行”口径混乱的问题，改为分别展示实际运行数、有效运行上限和管理员设置并发数。
- 后台缓存状态输出前会按真实运行任务自校准运行槽，避免异常路径造成运行数量显示不准或调度被假运行数卡住。
- 限速分片计算改为按有效并发估算，保守模式同账号单任务时不再被设置并发数误压低。

### 调整

- 缓存信息说明补充保守模式规则：同一 Telegram 账号只运行 1 个后台缓存任务，多个账号或高速模式才会按并发数量并行。

## 版本 2.0.27

### 修复

- 修复后台缓存 `running` 任务 `0 B/s` 后长期占用运行槽的问题。
- 停滞判定改为基于真实写盘时间 `lastObservedAt`，不再被普通状态刷新或测速刷新误重置。
- 缓存速度诊断只统计真实运行中的后台任务，并输出 `lastProgressAt` / `lastObservedAt`，便于判断是否真实写盘。

### 调整

- 高速模式新增自保护：同账号多任务连续 `0 B/s` 时会自动降级为保守模式，并释放多余任务重新排队。

## 版本 2.0.26

### 修复

- 根据最新运行日志修复保守模式下同一 Telegram 账号仍会并发缓存多个视频的问题。
- 保守模式现在强制同账号最多 1 个后台缓存任务运行，减少 Telegram 连接反复断开重连。
- 后台缓存监控新增完成收口：`.silent.part` 文件达到视频大小后会直接完成并释放运行槽，避免临近完成时长时间显示 `running`。

### 调整

- 高速模式保留多任务并发能力，由管理员手动开启。

## 版本 2.0.25

### 修复

- 修复后台缓存停滞后占用运行槽，导致前台聊天记录一直加载中的问题。
- 保守模式下前台会话、消息、搜索和发送会优先执行，后台群视频缓存会让出连接并重新排队续传。
- 后台缓存真实写盘停滞时会释放运行槽并重新排队，避免卡死后不再启动。
- 聊天列表、聊天记录和媒体读取增加 Telegram 连接超时提示，不再无限停留在加载状态。

### 新增

- 缓存信息新增“保守模式 / 高速模式”：默认保守，管理员可手动开启高速模式。
- 缓存信息列表支持多选和批量取消异常或不需要的后台缓存任务。

## 版本 2.0.24

### 修复

- 修复升级安装时旧 Node 后端进程未重启，导致新前端连接旧后端的问题。
- 启动脚本新增版本检测，安装新版本时会自动停止旧进程并启动新服务。
- 启动脚本会按 Feigram app 路径清理残留 Node 进程，避免 PID 文件丢失时旧服务继续占用 Telegram session。
- Telegram 账号连接增加账号级连接锁，启动恢复、后台任务和前端请求不会再同时为同一个账号创建多个 GramJS client。
- 遇到 `AUTH_KEY_DUPLICATED` 时会先断开旧连接并重试；如果 Telegram 已判定 session 失效，会提示重新登录账号。

## 版本 2.0.23

### 修复

- 修复后台缓存 `406: AUTH_KEY_DUPLICATED` 导致 Telegram 连接被判定重复的问题。
- 后台缓存不再创建独立 Telegram 连接，改为复用账号唯一连接，避免同一 session/auth key 被并发登录。
- 会话列表加载失败时会在前端直接展示错误和重新加载按钮，不再只显示空白或“暂无会话”。

## 版本 2.0.22

### 修复

- 修复后台缓存 `Cannot cast Document to any kind of InputFileLocation` 的问题。
- 后台缓存和缓存速度诊断会把 Telegram `Document` 转换为 `InputDocumentFileLocation` 后再读取。
- 在线视频播放路径也统一使用明确的 Telegram 文件定位对象，避免同类类型转换错误。

## 版本 2.0.21

### 修复

- 修复后台缓存 `400: CONNECTION_NOT_INITED`、`401: AUTH_KEY_UNREGISTERED` 后无法继续续传的问题。
- 后台缓存下载链路从手工 `getSender/invokeWithSender` 改为 GramJS `iterDownload`，使用库内置的下载重试和 sender 管理。
- `400/401`、`Not connected`、`TIMEOUT` 等连接类错误会重置缓存专用连接并重新排队续传。
- 任务重新进入运行状态时会清空旧错误，避免正在缓存仍显示历史 `400/401`。
- 运行诊断页优化缓存速度诊断和飞牛日志区域高度，飞牛日志不会再被测速窗口挤压。

## 版本 2.0.20

### 修复

- 复盘飞牛日志中的 `sender already has some hanging states. reconnecting` 后，修复后台缓存 Telegram sender 重连风暴问题。
- 撤销 2.0.19 的外层 `Promise.race` 超时，避免超时后底层 GramJS 请求仍残留在 sender 中。
- 群视频后台缓存改用独立 Telegram client，不再和前台聊天、消息更新共用连接。
- 真实写盘停滞时只重置缓存专用连接，然后从现有 `.silent.part` 文件续传，不影响前台会话。
- 确认服务端缓存清理为每天一次且保留至少 1 天，不会 3-5 分钟删除正在缓存的文件。

## 版本 2.0.19

### 修复

- 修复后台缓存停滞重排时被误显示为“已取消”的问题。
- 后台缓存不再强行并发重启同一个文件，避免旧任务和新任务同时写同一个 `.silent.part` 文件。
- 该版本曾加入外层本地超时；2.0.20 已撤销该实现，改为缓存专用连接隔离和真实写盘巡检。
- 真实写盘停滞时会显示“等待当前请求超时后续传”，不再误导为用户取消。
- 后台缓存状态机调整为更接近 Telegram 客户端的顺序续传模型。

## 版本 2.0.18

### 修复

- 修复后台缓存显示有速度但真实 `.silent.part` 文件不继续增长的问题。
- 缓存信息列表会按真实写盘文件大小刷新进度，避免内存进度和实际缓存脱节。
- 后台缓存巡检从 10 分钟缩短到 30 秒；真实写盘超过 90 秒无增长会自动重新排队。
- 运行速度超过 20 秒没有真实进度会自动清零，避免旧速度误导。
- 优化停滞重排的运行标记，旧 Telegram 读取任务和新排队任务不会互相影响并发计数。

## 版本 2.0.17

### 优化

- 缓存速度诊断默认改为聚合当前运行中的后台缓存任务速度，不再额外读取 Telegram 文件。
- 新增“抽样测速”入口，仅在需要深挖单文件/DC 速度时读取 1MB 样本。
- 避免诊断读取和 5 个后台缓存任务抢同一 Telegram 账号/DC 连接，导致测速结果显著低于真实后台缓存速度。

## 版本 2.0.16

### 修复

- 修复后台缓存直接调用 Telegram `upload.getFile` 时偶发 `LIMIT_INVALID` 的问题。
- Telegram 分片请求固定使用合法对齐分片，不再把末尾剩余字节作为 `limit` 直接传入。
- 遇到 `LIMIT_INVALID` 时会自动从 512KB 降级到 256KB、128KB、64KB 或 32KB 后继续请求，避免任务直接失败。

### 优化

- 缓存速度诊断新增请求分片、实际分片、降级次数和 `LIMIT_INVALID` 次数。
- 后续你把测速结果发回来时，可以直接判断是否触发了分片降级。

## 版本 2.0.15

### 调整

- 移除 FPK 中的 `ffmpeg` / `ffprobe` 运行时残留，进一步缩小安装包体积。
- 构建脚本不再下载或打包 ffmpeg 静态运行时。
- 服务端不再使用 ffmpeg 为已缓存视频兜底截帧。

### 说明

- 视频封面优先使用 Telegram 自带缩略图。
- 如果 Telegram 没有提供缩略图，Feigram 会显示无封面状态，不再本地转码/截帧。

## 版本 2.0.14

### 修复

- 后台群视频缓存不再使用 GramJS `iterDownload` 作为大文件续传路径，改为直接调用 Telegram `upload.getFile` 顺序分片。
- 修复 `.silent.part` 已下载较多时可能落入较慢下载路径，导致不限速仍只有几十 KB/s 的问题。
- 续传 offset 会按 Telegram 要求对齐到 4096 字节，避免旧分片尾部造成隐性失败。

### 新增

- 管理员后台“诊断”更名为“运行诊断”。
- 运行诊断新增“测试缓存速度”，可对当前后台缓存任务做 Telegram 读取测速。
- 测试结果会显示实测速度、样本大小、耗时、Telegram DC、当前限速、并发、运行任务和队列数量，便于继续定位缓存速度问题。

## 版本 2.0.13

### 调整

- 移除客户端内置 HLS/hls.js 播放器引用，视频播放模式仅保留原始在线播放和本地播放器。
- 移除服务端 HLS 播放路由，旧配置中的 HLS 播放模式会自动回退为原始视频在线播放。
- 保留 ffmpeg 作为视频封面兜底截帧工具，不再作为 Feigram 播放器模式暴露。

### 方案说明

- Gopeed 可作为通用下载器参考，但不直接处理 Telegram MTProto 下载、fileReference 刷新和 Telegram session。
- Telegram 专用下载器如 tdl 更接近底层需求，但会引入独立登录、session 管理、发布许可和进程运维成本。
- 当前后台缓存继续使用 Feigram 内置 GramJS 可续传下载链路，避免重复引入之前已修复的缓存、限速、排队和断点续传问题。

## 版本 2.0.12

### 修复

- 修复后台缓存大视频时 `FILE_REFERENCE_EXPIRED` 反复失败的问题。
- 修复后台缓存重试前删除 `.silent.part`，导致隔夜缓存反复从 0 开始的问题。

### 优化

- 后台缓存 document 视频改为可续传下载，失败后会保留分片并从已下载位置继续。
- 遇到 Telegram 文件引用过期时，会重新拉取消息刷新 `fileReference`，并在短时间内重新排队继续缓存。
- 缓存进度、速度和限速计算会从已存在分片大小继续。

## 版本 2.0.11

### 新增

- 缓存信息最大缓存速率确认支持“不限速”。
- 后台缓存并发数量新增 `10` 选项。
- 新增 [缓存与下载逻辑](cache-download-logic.md) 文档，便于后续调整限速、并发、分片和重试策略。

### 优化

- 服务端将后台缓存并发强制限制在 1-10，避免前端或旧配置传入异常值。
- 限速模式下并发任务继续共享总速率限制；不限速模式下使用 Telegram 默认下载策略。

## 版本 2.0.10

### 修复

- 修复后台缓存暂停后重新开启，前几个运行任务仍显示“已暂停”且不继续缓存的问题。
- 修复重新开启后速率窗口沿用旧下载进度，导致缓存长时间处于低速的问题。

### 优化

- 重新开启后台缓存、调整限速或调整并发时，会重置限速窗口。
- 正在退出的旧暂停任务会标记为重新排队，退出后自动回到队列继续缓存。
- 优化动态下载分片策略，2MB/s、5 并发这类场景会使用更大的分片提高吞吐。

## 版本 2.0.9

### 修复

- 修复后台缓存设置为高并发、高限速时实际速度只有几十 KB/s 的问题。
- 限速模式下 Telegram 下载分片不再固定为 32KB，会根据最大缓存速率和并发数量动态调整。

### 优化

- 低速限速仍使用较小分片控制瞬时峰值；5MB/s、5 并发这类场景会使用更大的分片提高吞吐。
- 总速率限制仍然全局共享，不会因为并发数量提高而放大上限。

## 版本 2.0.8

### 修复

- 修复后台缓存恢复任务偶发提示“找不到会话”的问题。
- 后台缓存现在会直接通过 Telegram peer id 解析群组/频道，不再只依赖当前会话列表缓存。

### 优化

- 会话列表和 Telegram 文件夹同步拉取上限提高到 500，减少大号会话展示不全的情况。
- 会话确实不可访问时，错误提示改为说明可能已退出群组、会话已删除或 Telegram 暂时无法解析。

## 版本 2.0.7

### 新增

- 缓存信息新增后台缓存并发数量设置，可在 1-5 个任务之间选择。
- 并发任务继续共享最大缓存速率限制，避免把限速按任务数放大。

### 优化

- 调整并发数量后会立即重排当前后台缓存任务，多余运行任务会回到队列。

## 版本 2.0.6

### 修复

- 进一步增强群缓存限速策略，减少系统监控中出现明显冲破限速的情况。
- 限速开启时，后台缓存强制单任务运行，避免多个缓存任务并发叠加。
- 限速开启时，静默缓存改用 Telegram 底层下载接口，并指定 32KB 小分片，降低单个大分片造成的瞬时写入峰值。
- 从不限速切换为限速时，会主动暂停多余运行任务并重新排队。

## 版本 2.0.5

### 修复

- 修复群缓存最大缓存速率限制不准确的问题。
- 原先限速按单个后台缓存任务计算，多个并发任务会叠加突破上限；现在改为所有群缓存任务共享全局总速率。
- 移除单次节流最多 5 秒的限制，避免大分片下载时突破设定速率。
- 修改限速值时会重置限速窗口，新设置会立即作用于正在运行的后台缓存任务。

## 版本 2.0.4

### 修复

- 清理旧版本遗留的已取消群缓存记录，`cancelled` 状态不会再读回缓存信息列表。

### 改进

- 优化首次打开加载顺序：账号和会话优先加载，公告、关于、下载列表、缓存信息和文件夹延后加载。
- 管理员后台重构为“账号管理、服务端设置、缓存信息、隐私设置、诊断”。
- “账号管理”合并飞牛账号管理和 Telegram 账号管理。
- “服务端设置”合并原覆盖 API 设置、缓存下载设置和播放器设置。
- “缓存信息”替代原群缓存入口。
- “隐私设置”合并通知、隐私和分组设置。

## 版本 2.0.3

### 修复

- 群缓存任务点击取消后会从列表移除，并同步从持久化任务文件中删除。
- 下载列表遇到 `Request was unsuccessful 5 time(s)` 等 Telegram 网络请求失败时，会自动重试，降低偶发网络波动导致任务直接失败的概率。
- 群缓存使用同样的网络失败识别逻辑，遇到网络波动会重新排队继续缓存。

### 新增

- 群缓存新增排重，优先按同账号、同群、同视频标题和大小去重。
- 群缓存列表支持拖动排序，排序结果会保存到服务端，并作为后台缓存队列顺序。
- 服务端每 10 分钟巡检群缓存任务，失败、暂停、排队或长时间无进度的任务会重新拉起，直到缓存完成。

## 版本 2.0.2

### 修复

- 修复跳转到目标群聊后，消息列表尚未完成渲染时提前清空定位状态，导致无法精准滚动到目标消息的问题。
- 跳转目标消息无法从 Telegram 返回时，会提示“你要访问的内容已被删除”。

### 改进

- 群视频后台缓存控制状态持久化，刷新飞牛访问链接或重新打开窗口后仍能看到缓存任务状态。
- 管理员后台“群缓存”新增一键开启/暂停。
- 群缓存新增最大缓存速率限制，支持不限速、512 KB/s、1 MB/s、2 MB/s、5 MB/s、10 MB/s。
- 群缓存标题后新增 X，可单独取消未完成的后台缓存任务。
- 支持已同步会话中的 `t.me/c/...` 私有群组/频道消息链接在客户端内解析定位。

## 版本 2.0.1

### 修复

- 修复聊天消息中的 Telegram 链接在客户端内打开后，只切换群组但无法定位到目标消息的问题。
- 修复访问跳转消息后点击返回，无法恢复原聊天阅读位置的问题。
- 修复管理员后台诊断页读取日志时报 `handle.close is not a function` 的问题；该问题只影响诊断日志读取，不影响 Feigram 正常运行。

### 新增

- 管理员后台新增“群缓存”列表，专门展示群组信息页勾选自动缓存后产生的后台视频缓存任务。
- 群缓存列表展示视频标题、缓存进度、缓存速率、状态和更新时间，并保持紧凑布局。
- 群缓存任务继续和下载中心分离；只有用户主动点击缓存的视频才进入下载中心。

## 版本 2.0.0 - fnOS Client Edition

### 定位

- Feigram 2.0 开始按 fnOS Client Edition 方向整合，保留现有 Web + Node.js + FPK 技术路线。
- 目标是让 Feigram 在飞牛 OS 中更接近正式客户端体验：后台服务、日志诊断、更新检测、任务恢复、缓存管理和迁移记录。

### 新增

- 管理员后台新增“诊断”页，可查看版本、版本线、运行时间、缓存大小、任务数量、数据目录和日志尾部。
- 管理员后台新增应用内更新检测，连接 GitHub Releases 获取最新发布信息。
- 新增启动迁移记录，升级后会维护 `app-meta.json`、下载任务和后台缓存任务文件。
- FPK 启动脚本增强日志，记录启动时间、版本线、版本号、端口、数据目录和缓存目录。
- 用户主动下载任务和群组后台静默缓存任务支持落盘，服务重启或版本升级后自动恢复。

### 改进

- 群组后台静默缓存大于 100MB 视频时，最多同时运行 5 个任务，完成后再继续队列中的其他任务。
- 群组信息资源列表支持滚动加载更多，避免只能看到首屏资源。
- 下载任务 ID 改为稳定哈希，便于跨重启恢复和排重。

### 说明

- 主动点击“缓存”的视频仍进入下载中心。
- 群组勾选自动缓存产生的是后台静默任务，不进入下载中心。
- 如果底层 Telegram 下载库无法复用临时分片，恢复任务会自动重新拉取并覆盖临时文件，保证任务不丢失。

## 版本 1.5.9

### 修复

- 下载中心改为按任务创建时间固定排序，下载进度、速度和状态刷新不会改变任务位置。
- 去掉视频窗口上的播放器模式提示，减少聊天信息区域的视觉干扰。

### 新增

- 群组信息中的图片、视频、文件统计卡片可点击筛选资源。
- 群组信息资源区按类似官方客户端的媒体网格展示，可直接打开图片、播放视频或下载文件。
- 群组信息新增“后台自动缓存本群大于 100MB 的视频”开关。
- 视频消息新增封面预览和时长展示，优先读取 Telegram 自带缩略图，缓存后可由 ffmpeg 截帧兜底。

### 缓存策略

- 用户主动点击视频右上角“缓存”的任务仍进入下载列表。
- 群组信息里勾选后台缓存后，服务端会静默缓存最近消息中大于 100MB 的视频，不写入下载列表。
- 静默缓存会跳过已经在下载列表中排队、下载或完成的视频，避免重复拉取。

## 版本 1.5.8

### 修复

- 修复管理员后台表单、账户列表和设置页在部分窗口尺寸下被拉伸变形的问题。
- 修复群组信息侧栏中文件与媒体统计卡片占用大量空间的问题。
- 下载中心移除右键菜单，避免菜单无法自动消失；所有操作改为任务卡片内小按钮。
- 修复同一视频缓存完成后下载列表可能出现多条成功记录的问题。

### 改进

- 下载任务标题使用稳定省略展示，不再循环滚动或抖动。
- 下载速度、进度和更新时间始终可见，无需拖动或悬停。
- 默认播放器策略改为“原始视频在线播放优先”，内置 HLS 转码保留为实验兜底模式。
- 视频和图片消息按稳定最大宽度、横竖比例和媒体组网格展示，减少忽大忽小的跳变。

### 播放策略说明

- Telegram 官方客户端是本地客户端，可以直接使用本机解码能力和本地分段缓存。
- Feigram 运行在 Web 容器中，浏览器不支持的编码无法仅靠前端播放器解决。
- 因此 1.5.8 默认走原始视频在线播放，遇到无法解码的视频时，再由管理员切换到 HLS 转码或本地播放器模式。

## 版本 1.5.7

### 修复

- 修复视频下载失败后，HLS 播放器重试导致同一视频反复拉起下载任务的问题。
- 视频缓存下载改用 GramJS 官方 `downloadMedia` 流程，降低 `InputFileLocation` 转换兼容问题。
- 原始浏览器播放在没有本地缓存时不再走不稳定的远程 Range 分片。

### 新增

- 管理员后台新增播放器设置，可选择内置 HLS 转码、浏览器原始播放或本地播放器模式。
- 下载完成的视频可在下载列表中直接点击播放。
- 群组信息里的图片、视频、文件支持快捷访问。

### 改进

- 公告弹窗和管理员后台弹窗统一限制高度，内容区滚动，避免遮挡关闭按钮。
- 播放器显示当前播放模式，便于判断正在走内置转码还是浏览器原始播放。

## 版本 1.5.6

### 修复

- 修复视频缓存下载失败并提示 `Cannot cast Message to any kind of InputFileLocation` 的问题。
- 下载中心操作拆分为“清除列表记录”和“删除缓存文件”，清除列表不再误删已缓存文件。

### 新增

- FPK 内置 x86_64 Linux 静态 ffmpeg/ffprobe。
- 服务端新增 HLS 转码播放链路，将 Telegram 原始视频转成 H.264/AAC HLS 后供 Feigram 客户端播放。
- 点击聊天头部可打开群组信息侧栏，展示头像、名称、简介、成员/订阅数、媒体统计和最近文件。

### 改进

- 聊天消息气泡和媒体预览按聊天窗口宽度展开。
- 视频未点击加载时与播放时保持同一比例和显示尺寸。
- HLS 播放失败时会回退到原始 Range 播放链路。

## 版本 1.5.5

### 新增

- 左侧功能栏新增“下载”功能区，所有点击缓存的视频会进入统一下载列表。
- 下载列表展示任务状态、下载进度、已下载大小、总大小和实时速度。
- 下载任务支持开始/继续、取消、删除，右键菜单和按钮操作都可用。
- 视频窗口右上角新增缓存按钮，展示缓存、排队中、下载中、已缓存等状态。

### 改进

- 视频缓存改为后台任务，不再阻塞当前聊天窗口。
- 缓存视频使用 `.part` 临时文件，取消后再次开始会从已写入位置继续。
- 在线播放继续使用 HTTP Range 分块响应，优先播放当前点击的视频。

### 已知限制

- Feigram 当前内置播放器基于浏览器媒体能力。仅替换 JS 播放器无法解码浏览器本身不支持的视频编码；要彻底提升兼容性，需要后续接入服务端 ffmpeg 转码或 HLS 输出。

## 版本 1.5.4

Feigram Public 是第三方开发的非官方 Telegram 客户端，仅适用于飞牛 OS / fnOS 设备。Feigram 不隶属于 Telegram、Telegram Messenger Inc. 或飞牛官方。

### 新增

- 文件夹展示改成左侧竖向分组栏，包含分组图标、名称、会话数量和编辑入口。
- 聊天搜索支持回车提交，输入内容后显示一键清除按钮。
- 群聊消息支持展示发言人头像、名称和 ID，可在管理员后台隐私设置里开关。
- 同一组 `groupedId` 的图片和视频会合并为媒体组网格展示。
- 视频下载不再跳新窗口，改为 Feigram 客户端内缓存；缓存完成后同一条消息优先使用本地缓存播放。

### 改进

- 在线播放去掉强制 `type` 约束，让浏览器自行嗅探视频格式，降低误判不支持的概率。
- 视频仍受浏览器原生解码能力限制。对不支持的编码，Feigram 会优先提供本地缓存播放链路，后续可继续接入转码模块。

## 版本 1.5.3

### 新增

- 公开仓库更新为 https://github.com/g-star1024/Feigram-Public，应用内隐私政策和服务条款跳转到仓库最新文档。
- 文件夹同步改为后端按 Telegram dialog filter 计算会话归属，减少前端猜规则导致的分组失效。
- 视频预览和播放窗口保持同一尺寸，在线播放支持 HTTP Range 分块响应，减少等待完整下载。
- 管理员后台新增通知、隐私、分组设置，用于控制 Feigram 的桌面通知、消息预览、媒体预览、站内链接打开和文件夹展示。
- 内容刷新、机器人按钮回调和消息追加时尽量保留当前滚动位置，避免正在查看的内容跳走。

### 已知限制

- Telegram 官方客户端的完整通知、隐私和分组设置并不是一个可直接嵌入第三方 Web 客户端的模块。本版本同步的是 Feigram 客户端侧行为设置；Telegram 账号级隐私规则后续需逐项接入 MTProto 方法。

## 版本 1.5.1

### 新增

- 修复 Telegram 聊天文件夹同步，优先使用会话 folderId 归类，并保留 filter 规则兜底。
- 公告和关于 Feigram 改为随 GitHub 仓库发布，应用端只展示。
- 图片默认缓存，超过 100MB 的视频才缓存，小于等于 100MB 的视频不落盘缓存，其他文件类型不缓存。
- 视频改为点击后加载，优先播放当前点击的视频。

## 版本 1.5.0

### 新增

- 同步 Telegram 聊天文件夹，在左侧会话列表上方展示分组。
- 管理员公告栏，前台通知按钮会在有新公告时点亮。
- 已移除内置默认 Telegram API 配置；缺失自有凭据时安装向导/登录将拒绝启动，须于 my.telegram.org 申请 api_id / api_hash。
- 管理员后台保留“覆盖 API 设置”能力。
- 视频播放增加浏览器不支持格式时的下载兜底。
- 登录和管理员后台文案统一为“飞牛账户”。
- 补充隐私政策、服务条款和发布说明文档。

### 已支持能力

- 多飞牛账户隔离。
- 每个飞牛账户独立 Telegram session 和缓存目录。
- Telegram session 加密存储。
- 图片、视频、文件缓存下载位置管理。
- 聊天缓存自动清理。
- Telegram 链接客户端内跳转和返回。

### 已知限制

- Telegram 官方客户端的视频播放器不是可嵌入第三方 Web 客户端的组件。本项目使用浏览器原生播放器和 HTTP Range 分段响应；如果视频编码不被当前浏览器支持，请下载后用系统播放器播放。
- Telegram 聊天文件夹中部分动态规则由官方客户端本地组合，Feigram 会尽量同步显式包含、排除和常见类型规则。
- Feigram 不是官方 Telegram 客户端，不保证与官方客户端功能完全一致。

## 发布前检查

- 配置 HTTPS 或可信反向代理。
- 在管理员后台配置支持邮箱、隐私政策 URL、服务条款 URL。
- 检查缓存目录权限和可用磁盘空间。
- 确认使用者知晓这是第三方非官方客户端。
